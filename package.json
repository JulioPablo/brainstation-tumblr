{
  "name": "tumblr-practice",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "npm run clear",
    "build": "mkdir build\\css\\ && npm run copy && node-sass \".\\src\\sass\\main.scss\" \".\\build\\css\\main.css\"",
    "copy" :"(robocopy .\\src .\\build /e /xd sass) ^& IF %ERRORLEVEL% LEQ 1 exit 0",
    "start": " npm run build && http-server .\\build -o",
    "clear": "del /f /s /q \"build\" 1>nul && rmdir /s /q \"build\""
  },
  "author": "Julio",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.1.2",
    "font-awesome": "^4.7.0",
    "node-sass": "^4.9.2"
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "rimraf": "^2.6.2"
  }
}
